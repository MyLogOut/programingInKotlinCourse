package coreConcepts

fun main() {
    val one = 1
    val two = 2
    val first = 1
    val second = 2
    println("It is:\n${one==two} that one [$one] and two [$two] are equals")
    println("${one == first} that one [$one] and first [$first] are equals")
    println("${two == second} that two [$two] and second [$second] are equals")
}